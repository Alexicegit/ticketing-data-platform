version: 2
models:
  - name: fact_ticket_sales
    columns:
      - name: ticket_sale_key
        tests: [not_null, unique]
      - name: date_key
        tests: [not_null]
      - name: quantity
        tests: [not_null]
  - name: dim_date
    columns:
      - name: date_key
        tests: [not_null, unique]
